@extends('admin.layout')
@section('title', 'Pengaturan Situs')
@section('content')
<div class="admin-header">
    <h1 class="admin-header-sub">Pengaturan Situs</h1>
</div>
<p style="color:#6b7280;margin:0 0 18px;">Kontak dan media sosial yang ditampilkan di footer website.</p>

@if (session('status_situs'))
    <div style="background:#dcfce7;color:#166534;padding:12px 16px;border-radius:10px;margin-bottom:16px;">{{ session('status_situs') }}</div>
@endif

@php
    $kolom = [
        'kontak_alamat'      => ['Alamat', 'textarea'],
        'kontak_telepon'     => ['Telepon', 'text'],
        'kontak_email'       => ['Email', 'email'],
        'kontak_jam_layanan' => ['Jam layanan (satu baris per keterangan, kosongkan satu baris untuk memisahkan hari)', 'textarea'],
        'kontak_maps_embed'  => ['Link peta Google Maps (isi src dari kode "Sematkan peta")', 'url'],
        'sosmed_facebook'    => ['Link Facebook', 'url'],
        'sosmed_instagram'   => ['Link Instagram', 'url'],
        'sosmed_youtube'     => ['Link YouTube', 'url'],
    ];
    $gaya = 'width:100%;padding:10px 12px;border:1px solid #d6d3cd;border-radius:10px;background:#fff;font:inherit;box-sizing:border-box;';
@endphp

<form method="POST" action="{{ route('admin.pengaturan-situs.update') }}" style="background:#fff;border-radius:14px;padding:24px;box-shadow:0 1px 3px rgba(0,0,0,.06);max-width:760px;">
    @csrf
    @method('PUT')

    @foreach ($kolom as $nama => [$label, $tipe])
        <div style="margin-bottom:16px;">
            <label style="display:block;font-weight:600;margin-bottom:6px;">{{ $label }}</label>
            @if ($tipe === 'textarea')
                <textarea name="{{ $nama }}" rows="6" style="{{ $gaya }}">{{ old($nama, $s[$nama] ?? '') }}</textarea>
            @else
                <input type="{{ $tipe }}" name="{{ $nama }}" value="{{ old($nama, $s[$nama] ?? '') }}" style="{{ $gaya }}">
            @endif
            @error($nama)
                <div style="color:#b91c1c;font-size:13px;margin-top:4px;">{{ $message }}</div>
            @enderror
        </div>
    @endforeach

    <button type="submit" class="btn-admin">Simpan Pengaturan</button>
</form>
@endsection